#ifndef SECRETARY_H
#define SECRETARY_H

// Adjusted to use pointers for returning both ratios:
void find_ratios(int N, int simulations, double *optimal_ratio_best, 
                double *optimal_ratio_average);

#endif